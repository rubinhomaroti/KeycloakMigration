{
  "ConnectionStrings": {
    "LegacyMySql": "Server=127.0.0.1;Database=mydatabase;Uid=user;Pwd=password;Default Command Timeout=10000;CharSet=utf8;"
  },
  "Query": "Input MySQL query. Add {EXISTING_USERS} in your where clause to replace the existing Keycloak Server users",
  "Keycloak": {
    "BaseUrl": "http://localhost:8080/",
    "Realm": "realm-name",
    "ClientId": "your-client-id",
    "ClientSecret": "your-client-secret"
  }
}